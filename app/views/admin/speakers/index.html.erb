<h1>Listing speakers</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
  </tr>

<% @speakers.each do |speaker| %>
  <tr>
    <td><%=h speaker.name %></td>
    <td><%=h speaker.description %></td>
    <td><%= link_to 'Show', admin_speaker_path(speaker.id) %></td>
   
    <td>
<%= link_to 'Edit', edit_admin_speaker_path(speaker.id) %>
    </td>
    <td>
<%#= link_to 'Destroy', admin_speaker_path, :confirm => 'Are you sure?', :method => :delete %>
    </td>
    
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New speaker', new_speaker_path %>
